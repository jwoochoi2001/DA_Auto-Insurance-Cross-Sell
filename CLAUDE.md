# 실습 작업 규칙

- 모든 경로는 이 프로젝트 루트를 기준으로 한 상대경로를 사용한다.
- `data/raw/`의 원본 데이터는 수정하거나 덮어쓰지 않는다.
- 정제하거나 변환한 데이터는 `data/processed/`에 저장한다.
- 분석용 Python 파일은 `analysis/`에 둔다.
- 표는 `outputs/tables/`, 그래프는 `outputs/figures/`에 저장한다.
- 처음 실행하기 전에 실제 파일명, 구분자, 인코딩, 열 이름과 자료형을 확인한다.
- 코드는 처음부터 다시 실행해도 같은 결과를 만들 수 있도록 작성한다.
- 제외하거나 대체한 데이터가 있다면 적용 기준과 건수를 분석 출력에 기록한다.
- 식별자와 예측 시점 이후에만 알 수 있는 정보가 모델 입력에 포함되지 않도록 확인한다.
- 목표값을 직접 또는 간접적으로 알려주는 변수를 모델 입력에 사용하지 않는다.
- 학습 데이터에서 정한 변환 기준을 검증과 테스트 데이터에 동일하게 적용한다.
- 모델과 판단 기준을 정하기 전에 최종 테스트 데이터를 사용하지 않는다.
- 요청하지 않은 Markdown 문서, 노트북과 중간 파일을 만들지 않는다.
